# Environment Configuration
NODE_ENV=development
PORT=3001

# Mock Data Configuration (Enable this to test without PostgreSQL)
USE_MOCK_DATA=true

# Database Configuration (Only needed when USE_MOCK_DATA=false)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nutrient_tracker
DB_USER=postgres
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=24h

# CORS Configuration
FRONTEND_URL=http://localhost:5173
